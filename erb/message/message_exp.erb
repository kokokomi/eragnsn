@message_exp_up(charactorNum, expType, amount)
  #DIM charactorNum
  #DIMS expType
  #DIM amount
  PRINTFORML %CALLNAME:(charactorNum)%: %expType%+{amount} ({EXP:(charactorNum):(GETNUM(EXP, expType))})