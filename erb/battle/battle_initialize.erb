@battle_init
  #DIM lcount
  戦闘_ターン数 = 0
  FOR lcount, 0, party_get_member_count()
    CALL charactor_reset_for_battle_start(lcount)
  NEXT
  FOR lcount, 0, party_get_member_count()
    CALL kojo_戦闘_開始(lcount)
  NEXT
  CALL reset_elemental_orb